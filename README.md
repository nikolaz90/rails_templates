# Rails templates to use when running rails new.
This is just a start, but will see additions soon.

for the rails_template_rspec, run with
$ rails new my_project -T -d postgresql \
  -m https://raw.githubusercontent.com/nikolaz90/rails_templates/main/rails_template_rspec.rb
